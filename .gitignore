.env
.env.*
!.env.example
node_modules/
.DS_Store
*.log
dist/akf

# Generated by `deno task compile` (scripts/stamp-build.ts) — embeds the git
# commit + build date into `akf --version`. Regenerated every build.
cli/build_info.json

# Local SSH authorized keys for the akf ssh plugin — machine-local, never published.
.devcontainer/authorized_keys.pub

# >>> akf >>>
.akf/
# <<< akf <<<
.claude/settings.local.json
